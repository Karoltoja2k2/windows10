import React from "react";
import "./dynamicGrid.scss";

const ResizeWindow = (props: any) => {
    return (
        <div className="container">
            <div className="grid" style={{}}></div>
        </div>
    );
};

export default ResizeWindow;
